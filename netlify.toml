# Netlify config for Next.js. For a showcase deploy of the configurator,
# also set the environment variable NEXT_PUBLIC_DEMO=1 in the Netlify UI
# (Site settings -> Environment variables) — that enables the /demo route.
[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
